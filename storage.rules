rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Regra simplificada: Permite leitura e escrita para qualquer usuário autenticado
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
